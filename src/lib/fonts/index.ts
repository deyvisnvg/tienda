import { Satisfy, Bungee, Nosifer } from 'next/font/google'
 
export const satisfy = Satisfy({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-satisfy',
})

export const bungee = Bungee({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-bungee',
})

export const nosifer = Nosifer({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-nosifer',
})